{
    "name": "vaults-protocol",
    "scripts": {
        "compile": "npm run compile:contracts && npm run compile:frontend",
        "compile:contracts": "cd contracts; npm run compile",
        "compile:frontend": "bash compile-frontend.sh"
    }
}